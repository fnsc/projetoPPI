{% extends 'base.html.twig' %}

{% block title %}Editar {{dados.email}} {% endblock %}
{% block body %}
    <div id="call-to-action">
        <div class="container">
            <div class="row text-center">
                <h2>PetCare - Editar Usuário</h2>
                <hr>
                <br>
            </div><!-- /row /text-center-->
            <div class="text-center">
                <div class="row row-max-400">
                    <form class="form-horizontal" action="/salvar_edicao/{{dados.idUsuario}}" method="POST">
                        <div class="form-group">
                            <label for="inputEmail3" class="col-sm-2 control-label">Email</label>
                            <div class="col-sm-10">
                                <input name="email" type="text" class="form-control" placeholder="seudominio@seuservidor.com" id="email" value="{{dados.email}}">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="inputPassword3" class="col-sm-2 control-label">Senha</label>
                            <div class="col-sm-10">
                                <input name="senha" type="password" class="form-control" placeholder="senha" id="senha" value="{{dados.senha}}">
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-10">
                                <button type="submit" class="btn btn-success">Atualizar</button>
                            </div>
                        </div>
                    </form><!-- formEdicao -->
                </div><!-- /row /row-max-400 -->
            </div><!-- /text-center -->
        </div><!-- /container -->
    </div><!-- /call-to-action -->
{% endblock %}

